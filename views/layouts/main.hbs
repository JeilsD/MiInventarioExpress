<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Milnventario Express</title>
    <style>
        body { font-family: sans-serif; margin: 0; }
        header { background-color: #f4f4f4; padding: 10px 20px; border-bottom: 1px solid #ccc; display: flex; justify-content: space-between; align-items: center; }
        header h1 { margin: 0; }
        header nav ul { margin: 0; padding: 0; list-style: none; display: flex; align-items: center; }
        header nav li { margin-left: 15px; }
        header nav a { text-decoration: none; color: #007bff; }
        header nav button { background: none; border: none; color: #dc3545; cursor: pointer; font-size: 1em; }
        main { padding: 20px; }
        footer { text-align: center; padding: 20px; margin-top: 30px; border-top: 1px solid #eee; }
    </style>
</head>
<body>

    <header>
        <h1><a href="/" style="text-decoration: none; color: black;">Milnventario Express</a></h1>
        
        <nav>
            <ul>
                {{! Si el usuario ESTÁ autenticado... }}
                {{#if isAuthenticated}}
                    <li>
                        <strong>Hola, {{userEmail}}</strong>
                    </li>
                    <li>
                        <a href="/products">Productos</a>
                    </li>

                    {{! --- AÑADIDO: Enlace al Chat (Tarea 10) --- }}
                    <li>
                        <a href="/chat">Chat</a>
                    </li>
                    {{! --- FIN DE LO AÑADIDO --- }}
                    
                    <li>
                        {{! El Logout es un formulario para enviar por POST }}
                        <form action="/logout" method="POST" style="display: inline;">
                            <button type="submit">Cerrar Sesión</button>
                        </form>
                    </li>
                {{else}}
                    {{! Si el usuario NO ESTÁ autenticado... }}
                    <li>
                        <a href="/login">Iniciar Sesión</a>
                    </li>
                    <li>
                        <a href="/register">Registrarse</a>
                    </li>
                {{/if}}
            </ul>
        </nav>
    </header>

    <main>
        {{{body}}}
    </main>

    <footer>
        <p>&copy; 2025 - Tarea de Aplicación Web</p>
    </footer>

</body>
</html>